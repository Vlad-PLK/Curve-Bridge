{
  "name": "curve-bridge",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ curve-bridge",
    "create-local": "graph create --node http://localhost:8020/ curve-bridge",
    "remove-local": "graph remove --node http://localhost:8020/ curve-bridge",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 curve-bridge",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.84.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
